'use strict';

/**
 * @ngdoc function
 * @name webApp.controller:ScreensCtrl
 * @description
 * # ScreensCtrl
 * Controller of the webApp
 */
angular.module('webApp')
  .controller('ScreensCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
